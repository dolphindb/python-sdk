[pytest]
addopts = -sv
norecursedirs = tmp* setup compatibility_testing
markers =
    BASIC: basic case for download & upload
    ARROW: case with plugin<Arrow>
    130: case support in 130 & 200 & 300
    200: case support in 200 & 300
    300: case support in 300
